<Page Background="White" x:Class="BikesForRentWPF.Page_Agenda"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BikesForRentWPF"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page_Agenda">
    <StackPanel>
        <Label Content="Kies en start - en een einddatum" Width="220" Height="25" Margin="20, 20, 20, 20"/>
        <TextBlock Text="Startdatum" Width="220" Height="25" Margin="20, 20, 20, 10"/>
        <DatePicker SelectedDate="{Binding StartDate, Mode=TwoWay}"  DisplayDateStart="{x:Static sys:DateTime.Today}" Width="220" Height="25" Margin="20,0, 20, 20" />

        <TextBlock Text="Einddatum" Width="220" Height="25" Margin="20, 20, 20, 10"/>
        <DatePicker SelectedDate="{Binding EndDate, Mode=TwoWay}" DisplayDateStart="{Binding StartDate}" Width="220" Height="25" Margin="20,0, 20, 20"/>
        <Button x:Name="btn_Verder" Content="Verder" Click="btn_Verder_Click" Width="120" Height="21" Margin="20, 40, 20, 20"/>
    </StackPanel>
</Page>
